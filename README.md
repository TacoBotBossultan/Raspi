# Raspi

Este raspi.

Pentru run la `flask`

`.env` tre sa arate cv de genu
`flask_client/.env`

```
ASK_AI_API_KEY=LMAO
```

```bash
cd flask_client
pip install -r requirements.txt
#pui aici
nvim flask_client/.env
flask --app . --debug run
```

enjoy!!!
